<%= simple_form_for(
      [restaurant, review], 
      data: { 
        insert_in_list_target: "form", 
        action: "submit->insert-in-list#send"
      }
    ) do |f| %>
  <%= f.input :comment %>
  <%= f.input :rating %>
  <%= f.submit class: "btn btn-primary" %>
<% end %>